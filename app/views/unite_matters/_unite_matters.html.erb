<table class="table table-striped mb-15pc">
  <thead>
    <tr class="header">

      <th class="header-border-left" width="45%"><%= t('name') %></th>
      <th width="40%"> <%= t('matters') %> </th>

        <th class="header-border-right" width="15%">
          <center>
            <%=t '.actions', :default => t("helpers.actions") %>
            <div style="float:right; margin-right:3px; width:37px;">
              <%= link_to new_button.html_safe, new_unite_matter_path, :class => 'btn-default btn-sm',
                title: 'Adicionar', 'data-toggle' => 'tooltip', 'data-placement' => 'right'%>
            <div>
          </center>
        </th>


    </tr>
  </thead>
  <tbody>
    <% @unite_matters.each do |unite_matter| %>
      <tr data-href="<%= unite_matter_path(unite_matter) %>">
        <div class="form_table">

          <td><%= unite_matter.name %></td>

          <td><%= unite_matter.matter_codes.join(", ") %></td>

            <td id="actions">
              <center>

                <%= link_to edit_button.html_safe,
                            edit_unite_matter_path(unite_matter), :class => 'btn btn-default btn-sm simple_button',
                            title: 'Editar', 'data-toggle' => 'tooltip', 'data-placement' => 'right' %>

                  <%= link_to remove_button.html_safe, unite_matter_path(unite_matter), :method => :delete,
                              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Você tem certeza?')) },
                              :class => 'btn btn-danger dropdown-toggle btn-sm disable',
                              title: 'Remover', 'data-toggle' => 'tooltip', 'data-placement' => 'right' %>

              </center>
            </td id="actions">


        </div>
      </tr>
    <% end %>

  </tbody>
</table>
