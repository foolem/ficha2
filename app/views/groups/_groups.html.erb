<table class="table table-striped">

  <thead>
    <tr class="header">
      <th class="header-border-left center" width="20px"><%= t('code') %></th>
      <th class="center" width="5%"><%= t('class') %></th>
      <th ><%= t('matter') %></th>
      <th class="center" width="110px"><%= t('semester') %></th>
      <% if !current_user.teacher? %>

        <th class="header-border-right center" width="130px"> <%=t '.actions', :default => t("helpers.actions") %>

          <%= link_to new_button.html_safe, new_group_path, :class => 'btn-default btn-sm', :style => 'margin-left:30px',
            title: 'Adicionar', 'data-toggle' => 'tooltip', 'data-placement' => 'right' %>

        </th>

      <% end %>

    </tr>
  </thead>

  <tbody>
    <% @groups.each do |group| %>
      <tr data-href="<%= group_path(group) %>">
        <td class="center"><%= group.matter.code %></td>
        <td class="center"><%= group.name %></td>
        <td><%= group.matter.name %></td>
        <td class="center" ><%= group.semester.semester_with_year %></td>
        <td id="actions" class="center">
          <%= link_to edit_button.html_safe,
                edit_group_path(group), :class => 'btn btn-default btn-sm simple_button',
                title: 'Editar', 'data-toggle' => 'tooltip', 'data-placement' => 'right' %>

          <%= link_to remove_button.html_safe,
                group_path(group),
                :method => :delete,
                :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Você tem certeza?')) },
                :class => 'btn btn-sm btn-danger disable',
                title: 'Remover', 'data-toggle' => 'tooltip', 'data-placement' => 'right'%>


        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<div class="center">

  <nav aria-label="Page navigation">
    <ul class="pagination pagination-sm no-mt-mb" id ="group_pages">
      <%= render_pages(@elements, @page, "/groups", group_page_length).html_safe %>
    </ul>
  </nav>

</div>
