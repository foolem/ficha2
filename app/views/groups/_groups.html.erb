<table class="table table-striped">

  <thead>
    <tr class="header">
      <th class="borderHeaderL" width="20px"><center><%= t('code')  %></center></th>
      <th width="5%"><center><%= t('class')  %></center></th>
      <th ><%= t('matter') %></th>
      <th width="110px"><center><%= t('semester')  %></center></th>
      <% if !current_user.teacher? %>

          <th class="borderHeaderR" width="130px"> <center> <%=t '.actions', :default => t("helpers.actions") %>
          <%= link_to new_button.html_safe, new_group_path, :class => 'btn-default btn-sm', :style => 'margin-left:30px',
            title: 'Adicionar', 'data-toggle' => 'tooltip', 'data-placement' => 'right' %>

      <% end %>
      </center></th>

    </tr>
  </thead>

  <tbody>
    <% @groups.each do |group| %>
      <tr data-href="<%= group_path(group) %>">
        <td><center><%= group.matter.code %></center></td>
        <td><center><%= group.name %></center></td>
        <td><%= group.matter.name %></td>
        <td><center><%= group.semester.semester_with_year %></center></td>
        <td id="actions"> <center>
          <%= link_to edit_button.html_safe,
                edit_group_path(group), :class => 'btn btn-default btn-sm simple_button',
                title: 'Editar', 'data-toggle' => 'tooltip', 'data-placement' => 'right' %>

          <%= link_to remove_button.html_safe,
                group_path(group),
                :method => :delete,
                :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Você tem certeza?')) },
                :class => 'btn btn-sm btn-danger disable',
                title: 'Remover', 'data-toggle' => 'tooltip', 'data-placement' => 'right'%>

          </center>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>


<center>
  <nav aria-label="Page navigation">
    <ul class="pagination pagination-sm noMarginTop" id ="group_pages">
      <%= render_pages(@elements, @page, "/groups", @length).html_safe %>
    </ul>
  </nav>
</center>
