<div>
  <%= render 'table' %>
</div>

<center>
  <nav aria-label="Page navigation">
    <ul class="pagination pagination-sm">
      <%= render_pages(@elements, @page, "/users/teachers", @length).html_safe %>
    </ul>
  </nav>
</center>
