<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<div class="login-page">
  <div class="form-login">

    <% if(current_user.admin?) %>
    <%= f.select(:role, [['Professor', :teacher], ['Avaliador', :appraiser], ['Administrador', :admin], ['Secretário', :secretary]]) %>
    <% end %>

    <%= f.text_field :name, placeholder: "name", autofocus: true, :autocomplete => "off"  %>

    <%= f.email_field :email , placeholder: "email address", :autocomplete => "off"  %>


    <%= f.password_field :password, autocomplete: "off", placeholder: "password" %>

    <br/>
    <%= f.password_field :password_confirmation, autocomplete: "off",placeholder: "confirm password" %>

    <button name="commit" data-disable-with="Signup" type="submit ">Update</button>

    <%= link_to back_button.html_safe + " Back", :back, :style => "float: left; margin-top: 15px" %>

  </div>
</div>

<% end %>
