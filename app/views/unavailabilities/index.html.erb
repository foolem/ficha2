<p id="notice"><%= notice %></p>

<h1>Unavailabilities</h1>

<table>
  <thead>
    <tr>
      <th>Availability</th>
      <th>Schedule</th>
      <th>Comments</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @unavailabilities.each do |unavailability| %>
      <tr>
        <td><%= unavailability.availability %></td>
        <td><%= unavailability.schedule %></td>
        <td><%= unavailability.comments %></td>
        <td><%= link_to 'Show', unavailability %></td>
        <td><%= link_to 'Edit', edit_unavailability_path(unavailability) %></td>
        <td><%= link_to 'Destroy', unavailability, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Unavailability', new_unavailability_path %>
